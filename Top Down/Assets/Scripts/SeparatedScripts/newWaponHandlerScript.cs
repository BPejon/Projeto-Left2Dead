using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class newWaponHandlerScript : MonoBehaviour
{
    //Esse script cuida, particularmente, de todos os aspectos relacionados ao controle da arma pelo jogador.
    //Aqui fazemos coisas como: Inventários e tudo mais. 
    //Não é aqui que controlamos a arma em si! Isto fica no GeneralWeaponScript.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
